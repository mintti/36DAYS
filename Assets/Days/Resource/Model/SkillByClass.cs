namespace Days.Resource.Model
{
    
    public enum WarriorSkill : byte
    {
        Lunge = 0,
        Swing
    }
    public enum ArcherSkill : byte
    {
        Shoot = 0,
    }
    public enum MagicianSkill : byte
    {
        FireBall = 0,
    }
    public enum BardSkill : byte
    {
        Gregorian = 0,
    }
}